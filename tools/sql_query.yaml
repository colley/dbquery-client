identity:
  name: sql_query
  author: colley
  label:
    en_US: SQL Query
    zh_Hans: SQL查询
description:
  human:
    en_US: Database SQL query
    zh_Hans: 数据库SQL查询
  llm: A Dify plugin tool to operate database query.
parameters:
  - name: query
    type: string
    required: true
    label:
      en_US: SQL Query
      zh_Hans: SQL查询
    human_description:
      en_US: The SQL query to execute (allow literal arg as parameters)
      zh_Hans: 要执行的SQL查询（参数使用Jinja 渲染表示，或使用字面值）
    form: llm
output_schema:
  type: object
  properties:
    data:
      type: array
      items: 
        type: object
    columns:
      type: array
      items: 
        type: string
    affected_rows:
      type: number
extra:
  python:
    source: tools/sql_query.py
